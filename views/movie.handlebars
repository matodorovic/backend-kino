<aection>
    <h1> {{ movie.attributes.title }} </h1>
    <div>{{  movie.attributes.intro }}</div>
    {{#if movie.attributes.image }}
    <img class="movie-img" alt="{{ movie.attributes.title }}" src="{{ movie.attributes.image.url }}">
    {{/if }}
</aecrion>